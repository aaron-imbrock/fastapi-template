
# AA FastAPI Template

A robust and sensible baseline for kick-starting any new FastAPI applications, `aa-fastapi-template` includes development dependencies from `dev-dependencies` as well as

## Building a package to release

    # Dependencies
    python3 -m pip install --upgrade pip
    pip install --upgrade setuptools build twine

    # Tag release
    git tag v0.1
    git push origin v0.1


    # Build a distribution (for releases, do this on main with a fresh tag)
    python3 -m build

    # To first test release that package
    twine upload --repository testpypi dist/*

    # To formally release that package
    twine upload dist/*

    # To see what the current version will be
    python -m setuptools_scm
